// import { mainStore } from "../store";
// import { get, derived } from "svelte/store";

// export const select = (name, selector) => {
// 	const store = mainStore.get()[name].state;

// 	return derived(store, (state) => {
// 		return selector(state);
// 	});
// };
